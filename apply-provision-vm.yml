- name: Terraform Apply
  hosts: terraform
  gather_facts: false

  tasks:
    - name: Terraform apply
      ansible.builtin.command:
        cmd: terraform apply -auto-approve /tmp/{{ vm_name }}.tfplan
        chdir: /opt/terraform/azure-vm